# Base URL for the recycling booking website
BOOKING_URL=https://example.com/recycling-booking.aspx

# Form parameters as JSON (keys = form field names)
# Add fields as you discover them from the ASPX form
FORM_PARAMS={"fieldName":"value"}

# Set to "false" to see the browser (useful for debugging)
HEADLESS=true
